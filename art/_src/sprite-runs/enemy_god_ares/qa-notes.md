# Ares motion QA

- `idle`: pass — four restrained breathing poses keep the same identity, spear, armour, proportions, and readable loop seam.
- `attack`: pass — high guard transitions cleanly into a committed leftward spear thrust; anatomy and equipment remain stable and no silhouette is clipped.
- `hit`: pass — the single recoil pose reads immediately; the automated low-motion warning is not applicable to a one-frame state.
- `death`: pass — kneeling failure transitions clearly into a grounded final pose with stable anatomy, armour, cloak, and spear.
- Shared 96-colour extraction palette, transparent-cell QA, declared frame counts, and atlas composition passed.
