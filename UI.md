# UI 가이드

게임 웹 UI의 기준. 새 UI는 여기 어긋나면 틀린 것이다.

## 레이아웃은 움직이지 않는다 — 제1규칙

화면이 흔들려도 되는 것은 **의도한 연출 애니메이션뿐**이다. 그 외 어떤 이유로도 — 컴포넌트가 조건에 따라 사라져서, 텍스트 길이가 달라져서, 이미지가 늦게 와서 — 이웃 레이아웃이 밀리면 안 된다. 밀리면 사용자가 누르려던 것이 도망간다.

- **사라지는 컴포넌트는 자리를 남긴다.** 조건부로 없어지는 것은 DOM에서 빼지 말고 `visibility: hidden`·`opacity: 0`으로 비우거나, 컨테이너에 `min-height`로 자리를 예약한다(`.node-hint`가 예다). `{cond && <X/>}`로 빼서 아래가 당겨 올라오게 하지 않는다. 대상은 **화면이 서 있는 동안 바뀌는 것**이다 — 마운트 시점에 확정되고 그 화면이 사는 동안 안 변하는 조건부(쉼터의 「강화」 칸)는 흔들림이 아니다.
- **쓸 수 없는 버튼은 지우지 말고 `disabled`로 죽인다.** 버튼이 사라지면 이웃 버튼이 그 자리로 와서, 같은 위치 연타가 다른 행동이 된다.
- **오버레이는 흐름 밖에 띄운다.** 컷인·발화·툴팁·배지는 `position: fixed/absolute` 층이다(`.fx`, `.voice`). 문서 흐름에 넣으면 뜰 때마다 판이 밀린다.
- **이미지는 자리부터 확보한다.** `aspect-ratio`(`.card-art`) 또는 고정 width/height. 로드되며 판을 밀면 안 된다.
- **바뀌는 숫자는 `font-variant-numeric: tabular-nums`.** 9→10이 되며 폭이 변해 이웃을 미는 것도 흔들림이다(`.promise em`, `.choice .rule`이 예다).
- **스크롤바로 안 민다.** 루트는 `clip`으로 잘려 있다 — 화면 안에 스크롤 영역을 새로 만들지 않는다.

## 움직이는 것은 transform과 opacity뿐

`width`·`height`·`top`·`left`·`margin` 애니메이션 금지 — 레이아웃을 다시 계산해 이웃을 밀고 프레임을 먹는다. 게이지는 `scaleX`(`.enemy .hp i`), 이동은 `translate`, 등장·퇴장은 `opacity`. 예외를 두려면 그 자리에 이유를 주석으로 남긴다(`style.css:123`이 예다).

## 입력은 도망가지 않는다

- 사용자가 곧 누를 요소에 무한 루프 이동을 걸지 않는다. 등장 연출은 짧게 끝나고 제자리에 선다.
- hover 반응은 `transform`만 — 크기·여백을 바꿔 이웃 카드를 밀지 않는다(부채꼴 리프트가 예다).

## 연출

- 화면 전환은 `App`의 한 곳에서만, 한 가지 방향 규칙으로 한다. 전환 쌍마다 연출을 새로 만들지 않는다.
- `useReducedMotion`을 존중한다. 연출을 꺼도 정보가 사라지면 안 된다 — 연출은 장식이지 유일한 전달 수단이 아니다.
- 층 질서: backdrop(-1) < 판 < fx(3) < voice(4). 새 층은 이 사이에 끼우고, 판을 가리는 층은 `pointer-events: none`이거나 닫는 길이 있어야 한다.
